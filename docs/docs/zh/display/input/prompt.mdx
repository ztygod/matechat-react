import PromptDemo from '../../../../components/Prompt/PromptDemo';
import { Prompts } from '@matechat/react';

# Prompt 提示
>用于展示一组预定义的问题或建议

## 示例效果
<div className="playground-wrapper">
  <PromptDemo />
</div>

```ts title="代码 示例"
import { Prompts, Prompt, PromptTitle, PromptDescription } from "@matechat/react";

export default function PromptDemo() {
  return (
    <Prompts
      style={{
        display: "flex",
        flexDirection: "column",
        gap: "20px",      // 每个之间间距
        width: "100%", 
      }}
    >
      <Prompt size="lg">
        <PromptTitle>🌟 大号提示</PromptTitle>
        <PromptDescription>适合在页面顶部醒目显示的重要信息。</PromptDescription>
      </Prompt>

      <Prompt size="md">
        <PromptTitle>📌 中号提示</PromptTitle>
        <PromptDescription>常用于普通提示或分块标题下方的补充说明。</PromptDescription>
      </Prompt>

      <Prompt size="sm">
        <PromptTitle>💡 小号提示</PromptTitle>
        <PromptDescription>适合在局部或列表里显示简短提示。</PromptDescription>
      </Prompt>

      <Prompt size="xs">
        <PromptTitle>✨ 超小提示</PromptTitle>
        <PromptDescription>可用于非常轻量的标签或注释。</PromptDescription>
      </Prompt>
    </Prompts>
  );
}

```

## Prompt 组件演示

<Playground
  code={`
import React from "react";
import { Prompts, Prompt, PromptTitle, PromptDescription } from "@matechat/react";

export default function PromptPlayground() {
  return (
    <div
      style={{
        background: "#fafafa",
        padding: "16px",
        borderRadius: "8px",
        border: "1px solid #eee",
        boxShadow: "0 2px 6px rgba(0,0,0,0.05)",
        maxWidth: "400px",
        margin: "0 auto",
      }}
    >
      <Prompts
        style={{
          display: "flex",
          flexDirection: "column",
          gap: "12px"
        }}
      >
        <Prompt size="lg">
          <PromptTitle>🌟 大号提示</PromptTitle>
          <PromptDescription>适合在页面顶部醒目显示的重要信息。</PromptDescription>
        </Prompt>

        <Prompt size="md">
          <PromptTitle>📌 中号提示</PromptTitle>
          <PromptDescription>常用于普通提示或分块标题下方的补充说明。</PromptDescription>
        </Prompt>

        <Prompt size="sm">
          <PromptTitle>💡 小号提示</PromptTitle>
          <PromptDescription>适合在局部或列表里显示简短提示。</PromptDescription>
        </Prompt>

        <Prompt size="xs">
          <PromptTitle>✨ 超小提示</PromptTitle>
          <PromptDescription>可用于非常轻量的标签或注释。</PromptDescription>
        </Prompt>
      </Prompts>
    </div>
  );
}
`}
  language="tsx"
  title="List 参数演示"
  style={{
    height: 700
  }}
/>

## 参数

| 参数               | 说明                         | 类型                          | 默认值   |
| ------------------ | ---------------------------- | ---------------------------- | ------- |
| `size`             | 设置提示尺寸                  | `"lg" \| "md" \| "sm" \| "xs"` | `"md"` |
| `className`        | 自定义样式类                  | `string`                      | -      |
| `children`         | 内容，一般包含标题和描述      | `ReactNode`                   | -      |